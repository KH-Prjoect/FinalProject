DROP TABLE BOARDREPLY;

CREATE SEQUENCE REPLY_NO_SEQ;


CREATE TABLE BOARDREPLY (
	REPLY_NO	NUMBER	PRIMARY KEY,
	REPLY_DELFLAG	VARCHAR2(2)	NOT NULL,
	REPLY_CONTENT	VARCHAR2(2000)	NOT NULL,
	REPLY_REGDATE	DATE	NOT NULL,
	BR_NUM	NUMBER	NOT NULL,
	REPLY_MEMBER_ID	VARCHAR2(500)	NOT NULL,
	CONSTRAINT BOARDREPLY_BR_NUM_FK FOREIGN KEY (BR_NUM) REFERENCES BOARDRECIPE (BR_NUM)
);

DROP TABLE BOARDREPLY;


SELECT * FROM BOARDREPLY ORDER BY  REPLY_NO DESC;

INSERT INTO BOARDREPLY
		VALUES(
		REPLY_NO_SEQ.NEXTVAL, REPLY_GROUPNO_SEQ.NEXTVAL, 1, 0, 'N', '댓글테스', SYSDATE, 1,'테스트'
		);
		



